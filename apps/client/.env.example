# Planet of Toys — client build-time environment variables.
# Vite exposes only variables prefixed with VITE_ to the frontend bundle.

# Meta Pixel id read at build time (Req 3.4)
VITE_META_PIXEL_ID=

# Backend API base URL
VITE_API_BASE_URL=http://localhost:4000
