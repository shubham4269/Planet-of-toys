# Planet of Toys admin — build-time environment variables.
# Vite exposes only variables prefixed with VITE_ to the frontend bundle.

# Backend API base URL (same backend as the storefront)
VITE_API_BASE_URL=http://localhost:4000
